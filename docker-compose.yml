version: '3'
services:
  robot:
    image: ruffsl/arm32v7_ros2:crystal-source
    volumes:
      - ./src:/root/robot_ws/src/walkaround
    working_dir: /root/robot_ws
    command: tail -F anything # keep it running